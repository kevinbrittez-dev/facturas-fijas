# Gradle Wrapper
Este directorio contiene la configuración del Gradle Wrapper.
Si falta el archivo binario del wrapper (gradle-wrapper.jar), genera el wrapper localmente con:
  ./gradlew wrapper
o ejecuta:
  flutter create .
en la raíz del proyecto para regenerar la carpeta android completa.
